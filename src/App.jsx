var React = require('react');
var ReactDOM = require('react-dom');

var GlobalContainer = require('./global/Container.jsx');

ReactDOM.render(
  <GlobalContainer />,
  document.getElementById('content')
);
